#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main (void)
{
	char c;

	c = "a";
	while (a <= "z") {
		if ((a <= "q" && c != "e" && c <= "z")
				putchar(c);
		C++;
	}
	putchar('\n');
	return (0)
}

