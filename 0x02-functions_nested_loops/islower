#include "main.h"
/**
* isalpha - functions to check if c is a letter, lower case or upper case
* @c: is the int that will use for the arguement of the function
* Return: 0
*/
int _isalpha(int c)
{
	if((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
	return(1);
	}
	else
	return(0);
}
