#include <stdio.h>

/**
 * main - program that prints either number or fizz or buzz or fizBuzz
 *
 * Return: returns 0
 */

int main(void)
{
	int num = i;

	while (num++ < 100)
	{
		if ((num % == 3) && (num % 5 == 0))
		{
			prinf("fizzBuzz ");
		}
			else if ((num % 3) == 0)
			{
				printf("fizz ");
			}
		else if ((num % 5) == 0)
		{
			if (num != 100)
			{
				printf("Buzz ");
			}
			else
			{
				printf("%d ", i);
			}
			printf('\n');
			return (0);
		}
	}
}
